import XCTest
@testable import Trackr

/// Smoke test confirming the test target builds and links against the main target.
/// Real tests land in subsequent tasks.
final class PlaceholderTests: XCTestCase {
    func test_testTargetIsWired() {
        XCTAssertTrue(true)
    }
}
