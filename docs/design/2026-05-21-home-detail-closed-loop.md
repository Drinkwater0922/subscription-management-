# Design: Home + Detail Closed Loop

Generated by /office-hours on 2026-05-21
Branch: master
Repo: Drinkwater0922/subscription-management-
Status: APPROVED
Mode: Startup
Target version: 1.1 (NOT 1.0.1 — 1.0.1 is the feedback-entry resubmission, scope locked)

## Problem Statement

PennyLoop's Home and Detail screens are thin. Home is a flat list with a
monthly-total hero; Detail is a field table (PLAN / CYCLE / CATEGORY /
STARTED / NEXT / STATUS / NOTES) plus PAUSE / DELETE. Compared to the
competitor Orbit, both screens under-deliver — Orbit's detail page carries
a color-coded price-history log, a clear renewal state, and a cancel
action; its home groups free trials separately and shows relative renewal
timing.

## The Reframe — what the user is actually here to do

"Product completeness" is a builder-side feeling, not a user job. The real
job, and the one the subscription-fatigue trend sharpens, is:

> "This recurring charge — do I keep paying for it or not?"

That decision is per-subscription and it happens on the **Detail** screen.
Home's job is **triage**: surface what needs attention this month so the
user knows which Detail screen to open.

The product is a two-step loop:

- **Home answers:** what should I look at this month?
- **Detail answers:** what do I do about this one?

Today the loop is broken at both ends — Home doesn't triage (it just lists),
and Detail doesn't support a decision (it just displays fields).

## Premises

1. The goal is the **keep/kill decision**, not screen richness. Every new
   element on either screen must map to that job or to triage toward it.
   Decoration that doesn't is cut. — agreed.
2. Home and Detail ship together as one closed loop in v1.1. The user
   chose this over a detail-first split: with Claude Code the marginal
   cost of the second screen is low, and a single visible
   completeness jump is worth more before the next App Store push. — agreed.
3. Borrow Orbit's **information architecture** (price history, cancelled
   state, free-trials grouping, relative renewal timing), never its
   visual style. PennyLoop stays pixel-art: black + lime + VT323. No
   planet animation, no glossy gradients. — agreed.
4. The Calendar screen is out of scope here. It is a *new* surface;
   Home/Detail are *existing* surfaces and their thinness is the more
   acute wound. Calendar stays in ROADMAP Phase 2. — agreed.

## Approaches Considered

### Approach A: Home-first
Make Home a monthly console; leave Detail as-is. Completeness 5/10.
Rejected — the user notices something needs attention but tapping in still
dead-ends in a field table. The loop stays broken at the decision end.

### Approach B: Detail-first
Make Detail a decision page; leave Home as-is. Completeness 7/10.
The office-hours recommendation — delivers the actual decision job at the
lowest cost, validates the decision-page design before reworking Home.
Not chosen.

### Approach C: Both pages, closed loop (CHOSEN)
Home becomes triage, Detail becomes decision, shipped together in v1.1.
Completeness 9/10. Larger scope, but the loop is whole on first release.

## Recommended Approach — C, specified

Everything below renders in the existing PennyLoop pixel-art system
(`TrackrColors`, `PixelText`, VT323). No new visual language.

### Home — "this month" triage console

Keep: `● PENNYLOOP` wordmark, the FAB, the existing nav to Settings/Insights.

Change the hero. Today it is `MONTHLY · USD` + a single total. Make it
answer "what hits my card, and when":

- **This month's remaining charges** — sum of renewals from today through
  end of month. This is the number that drives anxiety.
- **Next charge** — name + date + amount of the nearest upcoming renewal.

Group the list instead of one flat sort:

- **FREE TRIALS** group, surfaced first. Trials are the highest
  cancel-anxiety item ("forgot to cancel before it converted"). They earn
  their own section.
- **ACTIVE** group below.
- Optional later: group-by billing cycle (monthly / yearly) — defer unless
  trivial.

Each row gains a **relative renewal line**: "RENEWS IN 3 DAYS" alongside
the existing date. Orbit does this; it reads faster than a bare date.

What Home does NOT get: stat-card grids for their own sake, charts, the
planet motif. Triage is the job; four decorative cards is not.

### Detail — "single-subscription decision" page

Restructure `SubscriptionDetailView` around the keep/kill decision:

- **Header** — brand icon + name + amount, set larger than today. The
  identity of the thing you're deciding on.
- **Renewal countdown** — "RENEWS IN 3 DAYS" as a prominent line, not
  buried in the field table. Time pressure is decision-relevant.
- **Fields** — cycle, category, started, next, status. Keep, but secondary
  to the countdown and price history.
- **Price history** — the highest-value add. PennyLoop already runs
  price-change detection; the data exists. Render a per-subscription
  history list with color-coded deltas: lime for a decrease, warn/red for
  an increase, each row `date · ↑/↓ delta · new price`. This directly
  serves the "catch price changes" value prop and answers "is this getting
  more expensive?" — a core input to keep/kill. Near-zero new
  infrastructure: surface existing data.
- **Notes** — keep.
- **URL** — make it a tappable link. Tapping opens the service's site /
  manage page so the user can act on the decision (go cancel, go
  downgrade).
- **Actions** — "MARK AS CANCELLED" distinct from "DELETE". Cancelled =
  the subscription ended but stays for history and insights; Delete =
  gone entirely. See Open Questions on reconciling this with the existing
  PAUSE action.

### The loop, concretely

Home: "Netflix · RENEWS IN 3 DAYS · ¥58" in the ACTIVE group → tap →
Detail: countdown + price history showing two increases this year +
tappable netflix.com + MARK AS CANCELLED. The user sees the charge coming,
sees it has crept up, and acts. Triage → decide.

## Open Questions

1. **PAUSE vs MARK AS CANCELLED.** PennyLoop's `Subscription` model and
   detail page currently have PAUSE. Cancelled and Paused are different
   states (Paused = temporarily inactive, still yours; Cancelled = ended).
   Decide one of: (a) keep both as distinct states, (b) replace PAUSE with
   CANCELLED, (c) keep PAUSE, add CANCELLED. This affects the
   `Subscription` model, `ProStatus`/status enums, snapshot tests, and
   how each state rolls into the monthly total. Resolve before eng work.
2. **Price-history storage.** Confirm whether per-subscription price
   changes are already persisted (SwiftData) or only computed transiently
   by the detection pass. If transient, the Detail price-history list
   needs a small persisted model. This is the one place "near-zero infra"
   may not hold — verify first.
3. **"Remaining this month" across currencies.** The hero sum must handle
   multi-currency subscriptions the same way the existing monthly total
   does (FX-pinned, home currency). Reuse `MonthlyTotalCalculator`'s
   currency handling, do not reinvent.

## Success Criteria

- Opening Home answers "what hits my card this month and when" without a
  tap.
- Free trials are visually separated from active subscriptions on Home.
- The Detail screen shows a renewal countdown and, for any subscription
  with a recorded price change, a color-coded price-history list.
- A user can go from "noticed on Home" to "decided and acted" (cancel /
  open manage page) without leaving the loop.
- All work renders in the existing pixel-art system; no new visual
  language is introduced.
- Snapshot test baselines updated for the new Home and Detail layouts.

## Distribution Plan

Covered by the existing App Store pipeline. This is a v1.1 update — Archive,
upload, submit. No new distribution channel. Ships only after 1.0.1
(feedback entry + rejection fixes) is approved and live.

## Dependencies

- Ships after v1.0.1 is approved by App Review. Do not bundle into the
  1.0.1 resubmission — that scope is locked.
- Open Question 2 (price-history persistence) must be answered before
  the Detail price-history work is estimated.

## The Assignment

Before any eng work: open the current build on an iPhone, go to one real
subscription's Detail screen, and write down the literal question you are
trying to answer in that moment. If it is not "do I keep paying for this,"
the reframe above is wrong and the spec needs to change. Validate the job
on the real screen with a real subscription first — 10 minutes, before a
line of code.

## What I noticed about how you think

- You said the 1.0.1 version is "做的比较简单的" and reached for a
  competitor to calibrate. Good instinct to look outward — but you
  pulled the *trend* and the *information architecture* from Orbit while
  explicitly rejecting its visual style. That separation (take the IA,
  keep your identity) is the hard part, and you did it without being told.
- You framed three approaches and picked the closed loop yourself. When
  the office-hours recommendation was the cheaper detail-first split, you
  overrode it with a reason — completeness jump before the next store
  push. That is a founder making a sequencing call with context the
  advisor does not have, not a builder taking the path of least
  resistance.
